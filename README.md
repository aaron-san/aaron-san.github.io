# Aaron's Portfolio Site
This site showcases some of Aaron's deployed web development projects.

See live site at https://aaron-san.github.io/.

## Development Workflow

### Commit and push source code to remote repository:
```bash
git add .
git commit -m "message"
git push
```

### Deployment

```bash
npm run build # compile the source code
npm run deploy # runs `gh-pages -d dist`
```





```shell

git add .
git commit -m "first commit"
git push origin main
```